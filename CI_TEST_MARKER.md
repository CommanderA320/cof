# CI test marker

Bu dosya, `.github/workflows/test-changed-files.yml`'in docs/pdfs/ dışı bir
değişiklikte doğru şekilde "hiçbir PDF değişmedi" sonucu verdiğini
doğrulamak için kullanılan, zararsız bir test dosyasıdır. Silinebilir.
